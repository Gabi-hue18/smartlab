import { useState } from 'react'
import { FileText, UploadCloud } from 'lucide-react'
export default function Documents(){const [files,setFiles]=useState([{name:'Oscilloscope_User_Manual.pdf',type:'Manual',asset:'EQP-001'},{name:'DMM_Calibration_Certificate.pdf',type:'Calibration',asset:'EQP-004'}]);function add(e){const f=e.target.files?.[0];if(f)setFiles(x=>[{name:f.name,type:'Uploaded',asset:'General'},...x])}return <><div className="page-heading"><div><p className="eyebrow">DOCUMENT CONTROL</p><h1>Documents</h1><p>Manuals, invoices, warranties, calibration certificates and service reports.</p></div><label className="primary-btn"><UploadCloud size={17}/> Upload<input hidden type="file" onChange={add}/></label></div><div className="doc-grid">{files.map((f,i)=><article className="doc-card" key={i}><FileText size={28}/><div><b>{f.name}</b><span>{f.type} · {f.asset}</span></div></article>)}</div></>}
