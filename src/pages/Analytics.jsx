import { useEffect, useMemo, useState } from 'react'
import { BarChart3, IndianRupee, Wrench, AlertTriangle } from 'lucide-react'
import { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid, LineChart, Line } from 'recharts'
import { getEquipment, getHistory, getMaintenance } from '../lib/store'
import MetricCard from '../components/MetricCard'
export default function Analytics(){const [e,setE]=useState([]),[h,setH]=useState([]),[m,setM]=useState([]);useEffect(()=>{Promise.all([getEquipment(),getHistory(),getMaintenance()]).then(([a,b,c])=>{setE(a);setH(b);setM(c)})},[]);const cost=h.reduce((s,x)=>s+Number(x.cost||0),0);const byLoc=useMemo(()=>Object.entries(e.reduce((a,x)=>{a[x.location]=(a[x.location]||0)+1;return a},{})).map(([name,value])=>({name,value})),[e]);const line=[{m:'Apr',cost:2100},{m:'May',cost:3650},{m:'Jun',cost:2800},{m:'Jul',cost:5100},{m:'Aug',cost:4300},{m:'Sep',cost:6200}];return <><div className="page-heading"><div><p className="eyebrow">DECISION SUPPORT</p><h1>Analytics</h1><p>Understand utilization, reliability and maintenance spend.</p></div></div><div className="metrics-grid"><MetricCard label="Tracked assets" value={e.length} icon={BarChart3}/><MetricCard label="Recorded spend" value={`₹${cost}`} icon={IndianRupee} tone="green"/><MetricCard label="Open work" value={m.filter(x=>x.status!=='completed').length} icon={Wrench} tone="orange"/><MetricCard label="Damaged" value={e.filter(x=>x.status==='damaged').length} icon={AlertTriangle} tone="red"/></div><div className="dashboard-grid"><section className="panel"><h3>Assets by laboratory</h3><ResponsiveContainer width="100%" height={280}><BarChart data={byLoc}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#22a06b" radius={[7,7,0,0]}/></BarChart></ResponsiveContainer></section><section className="panel"><h3>Maintenance cost trend</h3><ResponsiveContainer width="100%" height={280}><LineChart data={line}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="m"/><YAxis/><Tooltip/><Line type="monotone" dataKey="cost" stroke="#2f80ed" strokeWidth={3}/></LineChart></ResponsiveContainer></section></div></>}
